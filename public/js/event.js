$(document).ready(function () {

  // grab all users

  // dynamically create cards for each user
  // button links will eventually export user information to .vcf

  // get /event url to display event code

});